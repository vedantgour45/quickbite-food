# Base URL for the QuickBite backend API.
# Local dev points at the Express server on port 5000.
# In production, set this to the deployed Railway backend URL.
VITE_API_BASE_URL=http://localhost:5000
